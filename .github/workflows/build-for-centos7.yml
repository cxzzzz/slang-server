name: Build for CentOS 7 (Python Fix)

on:
  workflow_dispatch:
  push:
    branches: [ "main", "master" ]

jobs:
  build-centos7:
    runs-on: ubuntu-latest
    container: 
      image: centos:7
    steps:
    - name: 1. 修复 Yum 源并安装 Git
      run: |
        rm -f /etc/yum.repos.d/*.repo
        cat > /etc/yum.repos.d/CentOS-Vault.repo << 'EOF'
        [base]
        name=CentOS-7 - Base
        baseurl=http://vault.centos.org/7.9.2009/os/$basearch/
        gpgcheck=0
        
        [updates]
        name=CentOS-7 - Updates
        baseurl=http://vault.centos.org/7.9.2009/updates/$basearch/
        gpgcheck=0
        
        [extras]
        name=CentOS-7 - Extras
        baseurl=http://vault.centos.org/7.9.2009/extras/$basearch/
        gpgcheck=0
        
        [centos-sclo-sclo]
        name=CentOS-7 - SCLo sclo
        baseurl=http://vault.centos.org/7.9.2009/sclo/$basearch/sclo/
        gpgcheck=0
        
        [centos-sclo-rh]
        name=CentOS-7 - SCLo rh
        baseurl=http://vault.centos.org/7.9.2009/sclo/$basearch/rh/
        gpgcheck=0
        EOF
        
        yum install -y git
        git config --global --add safe.directory '*'

    - name: 2. 手动拉取代码及子模块
      run: |
        git config --global credential.helper store
        echo "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com" > ~/.git-credentials
        
        git clone https://github.com/${{ github.repository }}.git .
        git checkout ${{ github.sha }}
        git submodule update --init --recursive

    - name: 3. 安装编译工具 (GCC 11, CMake 3.28 & Python 3)
      run: |
        # 安装 EPEL 源
        yum install -y epel-release
        
        # 【关键修复】安装 Python 3 和 setuptools
        # slang 的脚本需要 Python 3.6+ 的语法
        yum install -y python3 python3-setuptools
        
        # 安装 GCC 11 和 Make
        yum install -y devtoolset-11-gcc-c++ make
        
        # 手动安装 CMake 3.28
        curl -L https://github.com/Kitware/CMake/releases/download/v3.28.0/cmake-3.28.0-linux-x86_64.tar.gz -o cmake.tar.gz
        tar -xzf cmake.tar.gz
        mv cmake-3.28.0-linux-x86_64 /opt/cmake
        ln -s /opt/cmake/bin/cmake /usr/local/bin/cmake

    - name: 4. 开始编译
      run: |
        source /opt/rh/devtoolset-11/enable
        
        # 【关键修复】强制 CMake 使用 Python 3
        # CentOS 7 默认 python 是 2.7，必须显式指定 Python 3
        export PYTHON_EXECUTABLE=$(which python3)
        
        mkdir build
        cd build
        
        cmake .. \
          -DPython3_EXECUTABLE=$PYTHON_EXECUTABLE \
          -DCMAKE_EXE_LINKER_FLAGS="-static-libgcc -static-libstdc++"
          
        make -j$(nproc)

    - name: 5. 上传构建产物
      uses: actions/upload-artifact@v4
      with:
        name: slang-server-centos7
        path: build/
