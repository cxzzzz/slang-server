name: Build for CentOS 7

on:
  workflow_dispatch:
  push:
    branches: [ "main", "master" ]

jobs:
  build-centos7:
    runs-on: ubuntu-latest
    container: 
      image: centos:7
    steps:
    # Step 1: 必须先安装 git，否则 checkout v2 无法运行
    - name: 1. 修复 Yum 源并安装 Git
      run: |
        # 强制删除系统自带的所有 repo 文件
        rm -f /etc/yum.repos.d/*.repo
        
        # 手动写入一个绝对正确的、指向 Vault 的源文件
        cat > /etc/yum.repos.d/CentOS-Vault.repo << 'EOF'
        [base]
        name=CentOS-7 - Base
        baseurl=http://vault.centos.org/7.9.2009/os/$basearch/
        gpgcheck=0
        
        [updates]
        name=CentOS-7 - Updates
        baseurl=http://vault.centos.org/7.9.2009/updates/$basearch/
        gpgcheck=0
        
        [extras]
        name=CentOS-7 - Extras
        baseurl=http://vault.centos.org/7.9.2009/extras/$basearch/
        gpgcheck=0
        
        [centos-sclo-sclo]
        name=CentOS-7 - SCLo sclo
        baseurl=http://vault.centos.org/7.9.2009/sclo/$basearch/sclo/
        gpgcheck=0
        
        [centos-sclo-rh]
        name=CentOS-7 - SCLo rh
        baseurl=http://vault.centos.org/7.9.2009/sclo/$basearch/rh/
        gpgcheck=0
        EOF
        
        # 安装 git (必须先装，给下一步 checkout 用)
        yum install -y git
        
        # 解决 git 检出时的安全目录问题
        git config --global --add safe.directory '*'

    # Step 2: 使用 checkout@v2 (兼容 CentOS 7 的 glibc 2.17)
    - name: 2. 拉取代码 (包含子模块)
      uses: actions/checkout@v2
      with:
        submodules: 'true' # 自动拉取 slang, reflect-cpp 等依赖
        fetch-depth: 0

    - name: 3. 安装编译工具 (GCC 11 & 手动安装新版 CMake)
      run: |
        # 安装 EPEL 和 GCC 11
        yum install -y epel-release
        yum install -y devtoolset-11-gcc-c++ make
        
        # 手动安装新版 CMake (3.28)
        curl -L https://github.com/Kitware/CMake/releases/download/v3.28.0/cmake-3.28.0-linux-x86_64.tar.gz -o cmake.tar.gz
        tar -xzf cmake.tar.gz
        mv cmake-3.28.0-linux-x86_64 /opt/cmake
        ln -s /opt/cmake/bin/cmake /usr/local/bin/cmake

    - name: 4. 开始编译
      run: |
        source /opt/rh/devtoolset-11/enable
        mkdir build
        cd build
        
        # 使用新版 CMake 编译
        cmake .. -DCMAKE_EXE_LINKER_FLAGS="-static-libgcc -static-libstdc++"
        make -j$(nproc)

    - name: 5. 上传构建产物
      uses: actions/upload-artifact@v4
      with:
        name: slang-server-centos7
        path: build/
